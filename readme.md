To get the app running, you need python 3.14 installed, run "pip install requirements.txt" and 
and you need an api key to put inside the .env\
e.g. HENRIK_API_KEY=HDEV-39...\
then start the app with the command "python -m ui.app"
make sure you are inside the StatsAppPremier folder when executing any of the commands